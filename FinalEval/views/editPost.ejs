<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post - Blog Management System</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav>
        <div class="container">
            <h1><a href="/" style="color: white; text-decoration: none;">Blog Management System</a></h1>
        </div>
    </nav>
    <main class="container">
        <div class="form-container">
            <h2>Edit Post</h2>
            <form action="/editPost/<%= post.id %>" method="POST" class="post-form">
                <div class="form-group">
                    <label for="title">Title</label>
                    <input type="text" id="title" name="title" value="<%= post.title %>" required>
                </div>
                <div class="form-group">
                    <label for="imageUrl">Image URL</label>
                    <input type="url" id="imageUrl" name="imageUrl" value="<%= post.imageUrl || '' %>" placeholder="https://example.com/image.jpg">
                    <small>Add or update the image URL (optional)</small>
                </div>
                <div class="form-group">
                    <label for="content">Content</label>
                    <textarea id="content" name="content" rows="10" required><%= post.content %></textarea>
                </div>
                <div class="form-actions">
                    <a href="/" class="cancel-btn">Cancel</a>
                    <button type="submit" class="submit-btn">Update Post</button>
                </div>
            </form>
        </div>
    </main>
</body>
</html>